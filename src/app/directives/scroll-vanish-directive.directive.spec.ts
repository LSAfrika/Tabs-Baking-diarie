import { ScrollVanishDirectiveDirective } from './scroll-vanish-directive.directive';

describe('ScrollVanishDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollVanishDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
