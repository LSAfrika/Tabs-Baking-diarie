// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,

  firebaseConfig : {
    apiKey: 'AIzaSyA--LHURCPn1OWzKBNR8UHD7mrF7FdUrwg',
    authDomain: 'baking-diaries-kenya.firebaseapp.com',
    databaseURL: 'https://baking-diaries-kenya.firebaseio.com',
    projectId: 'baking-diaries-kenya',
    storageBucket: 'baking-diaries-kenya.appspot.com',
    messagingSenderId: '638282638974',
    appId: '1:638282638974:web:5cd4940a767899cd'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
